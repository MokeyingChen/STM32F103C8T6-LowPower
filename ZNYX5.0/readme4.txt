v3.5新增
1.可调节音量
2.可主动去皮
3.修改完善了HX711函数
4.进入设置状态时只有设置时间改变了才修改时间，否则时间仍然为正常，防止进入设置时并不修改时间
。。。。
v3.7
4.修改esp，增加掉线或异常等重新连接。

v4.0
更改清空记录和清空定时的位置
完善注释
修改阈值存储机制，只有数值改变了才会重新存储
将定时界面下的key1与key2功能调换

v4.2
修改重连机制，每1或2秒发送一种重连指令，可以防止长时间阻塞系统，保证定时能响应（定时最长检测为5s）

v4.5
再次修改重连机制减少阻塞，
重排代码格式（整齐一点）

v4.6
1.修改数据上报机制，减少阻塞
2.修改自以来清除记录功能的bug，
case clearrecord:
			RecordStore_Clear();
			sprintf(newRecord, "%04d-%02d-%02d %02d:%02d %s", newdata[0], newdata[1], newdata[2], newdata[3], newdata[4], (Store_Data[1][5] == 2) ? "已服" : (Store_Data[1][5] == 1) ? "未服" : "无");。。。不能用newdata，newdata只有新定时发生时才有数据，而是Store_Data[1][0],
			Store_Data[1][1],
			Store_Data[1][2],
			Store_Data[1][3],
			Store_Data[1][4],。。否则会导致读取错误数据，导致卡死
3.修复在定时提醒时，只保存记录未保存闹钟数据（如闹钟的开关）

可能仍然存在问题：死机问题。1.在进行记录清除时有可能死机。可能原因：在flash擦除或写入时若被中断打断如串口接收数据可能会死机2.串口接收死机问题，接收数组不能太大
可能需要增加看门狗

v4.7
增加看门狗，防止意外死机